/**
 * A record of the navigation params for each route in your app.
 */
export type MainStackParamList = {
  Main?: {};
  BNR?: {
    //message: string;
  };
  BCR?: {
    //message: string;
  };
  Printer?: {
    //message: string;
  };
  IOboard?: {
   // message: string;
  };
  TopModule?: {
    //message: string;
  };
  CashBox?: {
   // message: string;
  };
  Recycler?: {
    //message: string;
  };
};
